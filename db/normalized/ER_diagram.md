```mermaid
erDiagram

    USER {
        UUID USER_ID PK
        TEXT LOGIN UK "NOT NULL"
        TEXT PASSWORD_HASH "NOT NULL"
        TEXT AVATAR_URL
        TEXT EMAIL UK "NOT NULL"
        TIMESTAMPTZ CREATED_AT "NOT NULL"
        TIMESTAMPTZ UPDATED_AT "NOT NULL"
    }

    ARTIST {
        UUID ARTIST_ID PK
        TEXT ARTIST_NAME "NOT NULL"
        TEXT AVATAR_URL
        TIMESTAMPTZ CREATED_AT "NOT NULL"
        TIMESTAMPTZ UPDATED_AT "NOT NULL"
    }

    GENRE {
        UUID GENRE_ID PK
        TEXT GENRE_NAME UK "NOT NULL"
        TIMESTAMPTZ CREATED_AT "NOT NULL"
    }

    TRACK {
        UUID TRACK_ID PK
        TEXT TITLE "NOT NULL"
        INTEGER DURATION_MS "NOT NULL"
        TEXT FILE_URL "NOT NULL"
        TIMESTAMPTZ CREATED_AT "NOT NULL"
        TIMESTAMPTZ UPDATED_AT "NOT NULL"
    }

    ALBUM {
        UUID ALBUM_ID PK
        TEXT TITLE "NOT NULL"
        TEXT AVATAR_URL
        UUID ARTIST_ID FK "NOT NULL"
        DATE RELEASE_DATE
        TIMESTAMPTZ CREATED_AT "NOT NULL"
        TIMESTAMPTZ UPDATED_AT "NOT NULL"
    }

    PLAYLIST {
        UUID PLAYLIST_ID PK
        TEXT TITLE "NOT NULL"
        TEXT AVATAR_URL
        TIMESTAMPTZ CREATED_AT "NOT NULL"
        TIMESTAMPTZ UPDATED_AT "NOT NULL"
        UUID USER_ID FK "NOT NULL"
    }

    TRACK_ARTIST {
        UUID TRACK_ID PK,FK
        UUID ARTIST_ID PK,FK
        TIMESTAMPTZ CREATED_AT "NOT NULL"
    }

    TRACK_GENRE {
        UUID TRACK_ID PK,FK
        UUID GENRE_ID PK,FK
        TIMESTAMPTZ CREATED_AT "NOT NULL"
    }

    TRACK_ALBUM {
        UUID TRACK_ID PK,FK
        UUID ALBUM_ID PK,FK
        TIMESTAMPTZ CREATED_AT "NOT NULL"
    }

    TRACK_PLAYLIST {
        UUID TRACK_ID PK,FK
        UUID PLAYLIST_ID PK,FK
        TIMESTAMPTZ CREATED_AT "NOT NULL"
    }

    USER_LIKED_TRACK {
        UUID USER_ID PK,FK
        UUID TRACK_ID PK,FK
        TIMESTAMPTZ LIKED_AT "NOT NULL"
    }

    USER ||--o{ PLAYLIST : creates
    USER ||--o{ USER_LIKED_TRACKS : likes

    ARTIST ||--o{ ALBUM : produces
    ARTIST ||--o{ TRACK_ARTIST : performs

    GENRE ||--o{ TRACK_GENRE : categorizes

    TRACK ||--o{ TRACK_ARTIST : has_artists
    TRACK ||--o{ TRACK_GENRE : has_genres
    TRACK ||--o{ TRACK_ALBUM : in_albums
    TRACK ||--o{ TRACK_PLAYLIST : in_playlists
    TRACK ||--o{ USER_LIKED_TRACKS : liked_by_users

    ALBUM ||--o{ TRACK_ALBUM : contains

    PLAYLIST ||--o{ TRACK_PLAYLIST : contains
    
    %%Redis
    
    USER ||--o{ REDIS_USER_SESSION : has_sessions
    USER ||--o{ REDIS_JWT_VERSION : has_token_versions
    
    REDIS_USER_SESSION {
        UUID USER_ID FK
        UUID SESSION_ID PK
        TEXT USER_AGENT
        TEXT IP_ADDRESS
        TIMESTAMPTZ CREATED_AT
        TIMESTAMPTZ LAST_ACTIVE_AT
        BOOLEAN IS_ACTIVE
    }
    
    REDIS_JWT_VERSION {
        UUID USER_ID PK,FK
        INTEGER CURRENT_VERSION "NOT NULL"
        TIMESTAMPTZ UPDATED_AT "NOT NULL"
    }
    
    REDIS_REFRESH_TOKEN {
        UUID SESSION_ID PK,FK
        TEXT HASHED_TOKEN "NOT NULL"
        TIMESTAMPTZ CREATED_AT "NOT NULL"
        TIMESTAMPTZ EXPIRES_AT "NOT NULL"
        BOOLEAN IS_REVOKED "NOT NULL DEFAULT false"
    }

    
    REDIS_CACHE_POPULAR_ARTISTS {
        TEXT TIME_RANGE PK "day|week|month"
        JSON ARTISTS_DATA "NOT NULL"
        TIMESTAMPTZ CACHED_AT "NOT NULL"
        TIMESTAMPTZ EXPIRES_AT "NOT NULL"
    }
    
    REDIS_CACHE_POPULAR_ALBUMS {
        TEXT TIME_RANGE PK "day|week|month"
        JSON ALBUMS_DATA "NOT NULL"
        TIMESTAMPTZ CACHED_AT "NOT NULL"
        TIMESTAMPTZ EXPIRES_AT "NOT NULL"
    }
    
    REDIS_CACHE_POPULAR_TRACKS {
        TEXT TIME_RANGE PK "day|week|month"
        JSON TRACKS_DATA "NOT NULL"
        TIMESTAMPTZ CACHED_AT "NOT NULL"
        TIMESTAMPTZ EXPIRES_AT "NOT NULL"
    }
    
    REDIS_RATE_LIMIT {
        TEXT ENDPOINT PK
        TEXT IDENTIFIER PK "user_id|ip"
        INTEGER REQUEST_COUNT "NOT NULL"
        TIMESTAMPTZ WINDOW_START "NOT NULL"
        TIMESTAMPTZ WINDOW_END "NOT NULL"
    }

    REDIS_USER_SESSION ||--|| REDIS_REFRESH_TOKEN : has_refresh_token
    REDIS_CACHE_POPULAR_ARTISTS }o--|| REDIS_ARTIST : caches_artists
    REDIS_CACHE_POPULAR_ALBUMS }o--|| REDIS_ALBUM : caches_albums
    REDIS_CACHE_POPULAR_TRACKS }o--|| REDIS_TRACK : caches_tracks